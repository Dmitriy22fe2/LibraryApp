version: '3.8'

services:
  test-postgres:
    image: postgres:16
    container_name: library_app_jdbc-postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: library_app_jdbc_db
    ports:
      - "5490:5432"